import React from "react";
import CityHome from "../CityHome";
import "./styles.css";

export default function Home() {
  return (
    <CityHome
      heading="Find a place to feel at home."
      note="A local voice for every renter. Explore this fictional community preview."
    />
  );
}
